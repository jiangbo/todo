{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "zig build",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}",
            },
            "problemMatcher": []
        }
    ]
}